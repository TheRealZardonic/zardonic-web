# TypeScript Errors Remaining

**Status: CLEAN** — Last checked: 2026-06-06

`npm run typecheck` (tsc -b --noEmit) passes with zero errors.

## History

This file previously tracked ~17 KB of TypeScript errors in test files
(primarily `VercelRequest` / `VercelResponse` mock type mismatches).

All errors were resolved by:
- Adding `as unknown as VercelRequest` casts where partial mocks were used in tests
- Replacing `vi.fn()` with properly typed `vi.fn<FunctionType>()` calls (Vitest 4 syntax)
- Using `register<TSchema>()` factory in `admin-action-registry.ts` to bridge the
  contravariance gap between generic `AdminAction<TSchema>` and the `AdminActionMap`

## Verification

Run the following to verify the clean state:
  npm run typecheck
  npm run test
  npm run build
  npm run lint
